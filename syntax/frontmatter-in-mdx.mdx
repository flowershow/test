---
title: Frontmatter fields access in MDX
version: "3.2.1"
status: stable
date: 2024-06-01
features:
  - a
  - b
  - c
---

> **Version:** {frontmatter.version} · **Status:** {frontmatter.status}

Released on {new Date(frontmatter.date).toLocaleDateString("en-GB", { year: "numeric", month: "long", day: "numeric" })}.

New features:
<ul>
  {frontmatter.features.map(feature => <li key={feature}>{feature}</li>)}
</ul>

