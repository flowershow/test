# "Unexpected character 'x' in attribute name" (PREVENTED)

```
The set \<a, b\> has two members

The set &lt;a, b&gt; has two members

The set < a, b > has two members

The set `<a, b>` has two members

\{a, b, c\}
```

The set \<a, b\> has two members

The set &lt;a, b&gt; has two members

The set < a, b > has two members

The set `<a, b>` has two members

\{a, b, c\}
